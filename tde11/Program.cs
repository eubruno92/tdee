// Quais os valores padrões dos tipos de dados bool, char, int, double, float, decimal e string.

// o valor padrão do tipo bool é false.
// o valor padrão do tipo char é \0, ou seja U+0000.
// o valor padrão de cada tipo integral (int) é zero, 0.
// o valor padrão de cada tipo flutuante (double),(float) e (decimal) é zero, 0.
// o valor padrão do tipo string é null(pelo que pesquisei pois não achei no nossos materiais)